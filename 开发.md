# AI 工作台 · 开发文档

个人知识 + 开发者工作台。一个项目同时挂本地目录和知识文档；中间对话，右侧编辑；对话可 `@文件` / `@知识`，产出是可保存的文档或代码补丁。

本文约定技术栈、仓库形态和开发流程。实现以本文为准；功能以 §1 全表为准，开发顺序以 §6.2 为准。

---

## 1. 产品范围

工作台要同时覆盖两件事：问自己的资料、改自己的代码。功能按模块列全，分期只决定先做哪一块，不把产品做成「只能聊两句的空壳」。

### 1.1 功能全表

| 模块 | 能力 | 分期 |
|---|---|---|
| 项目 | 创建 / 重命名 / 归档 / 删除；绑定本地目录；项目简介；项目规则文件（`AGENTS.md` 或 `.workbench.md`，对话自动带上） | 一 |
| 项目 | 导入 / 导出（会话 + 知识清单 + 设置，不含整个代码目录） | 二 |
| 对话 | 流式、选模型、停止生成、编辑重发、重新生成 | 一 |
| 对话 | `@文件` `@文件夹` `@知识` `@规则`；消息内 Markdown / 代码块；代码块「应用到画布」 | 一 |
| 对话 | 会话列表：搜索、重命名、置顶、归档、导出 Markdown | 一 |
| 对话 | 对话分支（从某条消息分叉）；收藏消息 | 二 |
| 命令面板 | `Ctrl+K`：切项目、搜会话、搜文件、搜知识、触发技能 | 一 |
| 全局搜索 | 一次搜会话标题/正文、文件名、知识标题 | 一 |
| 文件 | 文件树、打开、保存、未保存提示；项目内按文件名搜索 | 一 |
| 文件 | 内容搜索（本机 ripgrep 或等价）；忽略 `.gitignore` | 二 |
| 画布 | 多标签；Markdown / 代码；Monaco diff；确认后写盘；另存为 | 一 |
| 画布 | CSV/表格预览；图片预览；PDF 文本预览 | 二 |
| 知识 | 多文件上传（MD / TXT / PDF 抽文本）；标签；列表；`@知识` 附带切片 | 一 |
| 知识 | URL 抓取入库；SQLite FTS 检索；回答带来源卡片 | 二 |
| 知识 | 向量检索、引用高亮跳转原文 | 三 |
| 技能 | 内置：问文档、写说明、解释代码、起草提交说明、重构建议、找问题、会议纪要、翻译 | 一 |
| 技能 | 用户自建技能（人设 + 默认引用 + 提示词）；技能库增删改 | 二 |
| 工具 | 读文件、列目录（对话里由模型调用，写盘仍要确认） | 一 |
| 工具 | 网页搜索；抓网页；看图（把图片塞进多模态消息） | 二 |
| Git | 状态、查看 diff、AI 写提交说明、确认后 commit | 二 |
| 终端 | 项目目录下的本机终端，输出可引用进对话 | 二 |
| Agent | 多文件改动计划 → 逐文件 diff → 用户逐项确认 | 三 |
| 系统 | 设置页（Key 检测、默认模型、主题、字号）；亮/暗色；快捷键表 | 一 |
| 系统 | 用量（token / 次数 / 按日）；请求日志（可关） | 二 |
| 系统 | MCP 外部工具 | 三 |

### 1.2 第一期必须做成

- 项目 + 规则文件 + 绑定本地目录
- 对话全套（流式、停止、重发、`@` 引用、会话管理）
- 命令面板 + 全局搜索
- 文件树、多标签编辑、diff 确认写盘
- 知识上传（含 PDF 文本）+ 标签 + 附带上下文
- 八个内置技能
- 模型可调用「读文件 / 列目录」，写盘必须点确认
- 设置：模型、主题、快捷键说明

### 1.3 明确不做（所有期）

团队协作与权限、应用市场、调试器、可视化编排器、远程多租户 SaaS。

终端、Git、网页搜索放到第二期，不从第一期范围里删掉，只是后做。

### 1.4 成功标准

第一期：

- 丢进资料（含 PDF），能问出带出处的草稿，并能存成项目里的 Markdown
- 打开一个小仓库，`@` 若干文件后让 AI 改一处，diff 确认后文件已变
- `Ctrl+K` 能搜到项目、会话、文件、技能

第二期额外：

- 知识库能按关键词搜到，回答带来源
- 能看 Git 状态并用 AI 写提交说明后 commit
- 终端输出能贴进当前对话

---

## 2. 技术栈

本地优先：数据在本机 SQLite，文件走本机磁盘，模型走服务端转发。浏览器不持有 API Key。

### 2.1 总览

| 层 | 选型 | 用途 |
|---|---|---|
| 语言 | TypeScript（strict） | 前后端同一语言 |
| 前端 | Vite + React | SPA 工作台界面，开发时代理 `/api` |
| 后端 | Hono（Node） | 本机 HTTP API、SSE 流式、读盘 |
| 路由 | React Router | 前端页面（工作台为主屏） |
| UI | React + Tailwind CSS + shadcn/ui | 工作台布局与组件 |
| 图标 | lucide-react | 工具栏、文件类型图标 |
| 代码编辑 | Monaco Editor | 代码画布、diff 预览 |
| Markdown | 轻量 MD 编辑（textarea / CodeMirror MD） | 文档画布 |
| 状态 | Zustand | 当前项目、会话、面板 |
| 服务端 AI | Vercel AI SDK + `@ai-sdk/xai` | 流式对话、工具调用 |
| 模型提供方 | SpaceXAI（xAI API） | 默认 `grok-4.5` |
| 数据 | SQLite + Drizzle ORM + FTS5 | 项目、会话、消息、知识；第二期全文检索 |
| 文件 | Node `fs` | 读本地目录、读写画布产出 |
| PDF | `unpdf` 或等价抽文本 | 知识入库（第一期就要） |
| Git | `simple-git` | 第二期状态 / diff / commit |
| 终端 | `node-pty` | 第二期本机终端 |
| 校验 | Zod | 请求体、环境变量 |
| 包管理 | pnpm workspace | 根目录编排 `web` / `server` |
| 质量 | ESLint + Prettier + Vitest | 规范与单测 |

### 2.2 AI 接入（强制）

提供方名称是 **SpaceXAI**，实际走 xAI API，不要自造 `SPACEXAI_*` 或 `api.spacexai.*`。

| 项 | 值 |
|---|---|
| 环境变量 | `XAI_API_KEY`（只放服务端，gitignore） |
| Base URL | `https://api.x.ai/v1` |
| 默认模型 | `grok-4.5` |
| SDK | `ai` + `@ai-sdk/xai`（OpenAI 兼容客户端也可，优先 AI SDK） |
| 文档 | https://docs.x.ai |

对话接口必须流式（`streamText` / SSE）。Key 不得进客户端 bundle。

第一期只接 SpaceXAI。多模型切换做「同提供方下的模型名」，不在第一期接其他厂商。

### 2.3 数据与文件

- SQLite 文件放在用户数据目录（开发环境可用 `./data/app.db`）
- 项目绑定的本地目录路径写入数据库，内容不拷进库
- 上传的知识文件拷到 `./data/knowledge/<projectId>/`，原文保留
- 第一期知识用法：选中的文档切片后塞进 prompt；向量检索放到第二期
- 代码补丁：服务端生成 unified diff，前端 Monaco diff 预览，用户确认后再写盘

### 2.4 运行形态

第一期是 **本机双进程 Web 应用**：

- 前端 Vite：`127.0.0.1:5173`
- 后端 Hono：`127.0.0.1:3001`
- Vite 把 `/api` 代理到后端，浏览器只打前端源

需要读本地路径，两端都只监听 `127.0.0.1`。`pnpm dev` 同时拉起两者。

不用 Next.js：这是本机工具，不需要 SSR / RSC；文件和 Key 放在独立 Node 进程更干净。

不做 Electron/Tauri，直到第一期的对话和改文件稳定。

---

## 3. 架构

```
浏览器
  ├─ 左栏：项目 / 会话 / 文件树 / 知识
  ├─ 中栏：对话（流式）
  ├─ 右栏：多标签画布（MD / 代码 / diff）
  └─ Ctrl+K 命令面板、设置页
        │
        ▼
Hono 服务端（仅本机 :3001）
  ├─ /api/chat          SpaceXAI 流式（SSE）+ 读文件工具
  ├─ /api/projects      项目 CRUD、绑定目录、规则文件
  ├─ /api/files         列目录、读文件、搜索、应用补丁
  ├─ /api/knowledge     上传、标签、列表、附带上下文
  ├─ /api/skills        内置 / 自定义技能
  ├─ /api/search        全局搜索（会话 + 文件 + 知识）
  ├─ /api/git           第二期
  └─ /api/terminal      第二期
        │
        ├─ SQLite（Drizzle）
        ├─ 本地磁盘（项目目录 + knowledge）
        └─ SpaceXAI  https://api.x.ai/v1
```

原则：

- UI 不直接调模型；一律走 `/api/*`
- 写磁盘必须用户确认（应用补丁、保存画布）
- 上下文组装在服务端：系统提示 + 项目规则 + 技能 + 引用文件 + 引用知识 + 历史消息
- 模型工具只读；任何写盘、commit、跑命令都要用户确认

---

## 4. 仓库结构（目标）

```
demo9/
├── 开发.md                 ← 本文
├── package.json            ← pnpm workspace 脚本（dev/lint/test）
├── pnpm-workspace.yaml     ← packages: web, server, shared
├── pnpm-lock.yaml
├── .env.example            ← XAI_API_KEY=（只给 server 用）
├── .gitignore
├── data/                   ← 本地库与知识（不入库）
├── shared/                 ← Zod schema、API 类型
├── web/                    ← Vite + React
│   ├── index.html
│   ├── vite.config.ts      ← proxy /api → 127.0.0.1:3001
│   └── src/
│       ├── main.tsx
│       ├── pages/
│       ├── components/
│       └── store/
├── server/                 ← Hono + Drizzle
│   └── src/
│       ├── index.ts        ← 监听 127.0.0.1:3001
│       ├── routes/
│       └── lib/
│           ├── ai/         ← SpaceXAI 客户端、组 prompt
│           ├── db/
│           ├── files/
│           └── knowledge/
└── tests/
```

尚未建应用代码前，先落本文和 `.env.example`，再按流程脚手架。

---

## 5. 环境约定

```bash
# .env.example（放仓库根，server 读取；web 不读 Key）
XAI_API_KEY=
XAI_MODEL=grok-4.5
APP_HOST=127.0.0.1
SERVER_PORT=3001
WEB_PORT=5173
DATABASE_URL=file:./data/app.db
```

- Node.js 20+
- pnpm 9+
- 复制 `.env.example` 为 `.env`，填入 Key
- Key 从 https://console.x.ai 创建，文档见 https://docs.x.ai
- `XAI_API_KEY` 只存在于 `server` 进程，禁止 `VITE_` 前缀

常用脚本（脚手架后提供）：

| 命令 | 作用 |
|---|---|
| `pnpm dev` | 同时启动 Vite 与 Hono |
| `pnpm dev:web` | 仅前端 |
| `pnpm dev:server` | 仅 API |
| `pnpm build` | 两端生产构建 |
| `pnpm lint` | 静态检查 |
| `pnpm test` | 单测 |
| `pnpm db:generate` | Drizzle 生成迁移 |
| `pnpm db:migrate` | 执行迁移 |

---

## 6. 开发流程

### 6.1 总流程

```
定范围（本文 §1）
  → 拆任务（按模块，可独立演示）
  → 开分支 feat/<模块>
  → 先契约后 UI（Zod schema / API 形状）
  → 本机跑通 + 自测清单
  → 提交（见 §6.4）
  → 合并 main
  → 下一模块
```

一次只做一个可演示切片，禁止平行铺开六个模块却没有一条能点的路径。

### 6.2 推荐实现顺序

按依赖排列，后者依赖前者可点通。

| 顺序 | 切片 | 可演示结果 |
|---|---|---|
| 0 | 脚手架 | `pnpm dev` 出三栏空壳 + 设置页入口，无 Key 时明确报错 |
| 1 | 项目 | 建项目、绑定目录、写/读规则文件、刷新后还在 |
| 2 | 文件树 + 多标签编辑器 | 打开多个文件、保存、未保存标记 |
| 3 | 对话 | 流式聊、停止、编辑重发；会话重命名 / 置顶 / 搜索 |
| 4 | `@` 引用 | `@文件` `@知识` `@规则` 进入上下文 |
| 5 | 画布落盘 | 代码块应用到画布；diff 确认后写盘；另存为 Markdown |
| 6 | 知识 | 多文件 + PDF 抽文本、打标签、`@知识` |
| 7 | 技能 | 八个内置技能可从输入框或命令面板触发 |
| 8 | 命令面板 + 全局搜索 | `Ctrl+K` 搜项目 / 会话 / 文件 / 技能 |
| 9 | 只读工具 | 对话中模型可自行读文件、列目录（写仍确认） |
| 10 | 设置与主题 | 默认模型、亮暗色、快捷键说明 |

做完 0–3 才算「能跑」；做完 0–10 才算第一期完成。第二期从 Git、终端、知识 FTS、网页工具开始，另开切片，不插队。

### 6.3 日常开发

1. 从 `main` 拉最新：`git checkout main && git pull`
2. 开分支：`git checkout -b feat/chat-streaming`
3. 先写或改 `shared/` 里的 Zod schema，再写 Hono 路由，最后写 UI
4. 涉及模型调用：只改 `server/src/lib/ai/`，页面不拼 prompt
5. 涉及写盘：先做 dry-run / diff，确认按钮后再写
6. 自测 §6.5，再提交

### 6.4 分支与提交

- `main`：可运行的稳定线
- `feat/<短名>`：新能力
- `fix/<短名>`：缺陷
- 不直接在 `main` 堆未完成功能

提交信息（Conventional Commits）：

```
feat(chat): 接入 SpaceXAI 流式对话
fix(files): 应用 diff 时保留原文件换行
chore(db): 增加 knowledge 表迁移
```

允许的 type：`feat` `fix` `chore` `docs` `refactor` `test`。

### 6.5 合并前自测

每个切片至少覆盖：

- 无 `XAI_API_KEY` 时接口返回可读错误，不把堆栈甩到 UI
- 对话有首字延迟和逐 token 渲染（不能等整段结束）
- 刷新页面后当前项目仍在
- 绑定目录不存在时有提示，不崩
- 写文件只发生在用户点击确认之后
- `Ctrl+K` 能打开命令面板并搜到当前项目里的文件
- 上传 PDF 后能 `@` 到并出现在上下文里
- `pnpm lint` 通过

第一期完成时额外验证 §1.3 两条成功标准。

### 6.6 缺陷处理

1. 先复现路径（项目、文件、是否带知识）写成 issue 或提交说明
2. 能写单测的（diff 应用、切片、schema）先补测再改
3. 模型表现问题（胡编、不引用文件）优先改 `server/src/lib/ai/` 的上下文组装，不先怪 UI

---

## 7. 编码约定

- TypeScript `strict`，禁止无标注 `any` 扩散
- 服务端与客户端代码分开：读 `fs`、读 `XAI_API_KEY` 只放在 `server/`；`web/` 只打 `/api`
- 组件用函数组件；三栏布局是稳定骨架，不要改成卡片堆叠营销页
- 文案用中文；代码标识符用英文
- 注释只写非显而易见的约束（例如「写盘必须用户确认」）
- 新增依赖要能说清替代了什么，禁止随手加 SDK

---

## 8. 第二期 / 第三期

第二期（第一期完成后再排）：知识 FTS 与来源卡片、URL 入库、对话分支、网页搜索、看图、Git、终端、用量、自定义技能、内容搜索。

第三期：向量检索、多文件 Agent、MCP、定时任务。

未进入当前切片的项，不提前建空页面。API 路径可以按 §3 预留，但返回 501 即可，不要做假 UI。

---

## 9. 开工检查清单

- [ ] 已读本文 §1 范围和 §2 技术栈
- [ ] 已有 `XAI_API_KEY`（console.x.ai）
- [ ] Node 20+、pnpm 9+
- [ ] 按 §6.2 从切片 0 开始，不跳到技能、知识或 Git
